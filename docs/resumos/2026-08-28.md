# 28 de agosto de 2026 — o site ficou legível, bilíngue e mais vivo

Dia de acabamento. Nada de estrutura nova: o site já estava no ar desde ontem.
O que mudou foi quase tudo que se vê.

---

## Limpeza antes de começar

Apaguei `bryan-portfolio`, `bryan-portfolio-v2` e `bryan-portfolio-spider`.
Sobrou um portfólio só, este.

Vale registrar o que se foi junto: o `bryan-portfolio-spider` tinha histórico
de git só nesta máquina, sem GitHub. Os commits do M0 ao M3 do projeto Next.js
não existem mais em lugar nenhum. Foi decisão sua, com o aviso na mesa.

`meteoros` e `provedor-internet` ficaram — são sites no ar, não portfólio.

## O inglês entrou

Tem um seletor `PT | EN` no cabeçalho. A escolha fica guardada no navegador.

Antes disso o site já decide sozinho: navegador em português abre em
português, qualquer outro abre em inglês. Um recrutador de fora não precisa
clicar em nada.

Isso custou um dia porque o conteúdo já estava preparado desde o começo — cada
frase existe nos dois idiomas e o compilador recusa o build se faltar uma
tradução. A aposta de ontem se pagou hoje. O que faltava mesmo era o seletor e
tirar os textos de interface de dentro do código, que agora moram em
`src/content/ui.ts` sob a mesma regra.

## A queixa da legibilidade não era a cor

Você disse que as letras estavam difíceis de ler "por causa da cor e do
tamanho". A cor era a menor parte.

Os rótulos miúdos estavam a **10,4px com 0,3em de espaçamento**. Dois problemas
somados: abaixo de 12px a maioria dos navegadores já não desenha traço fino
direito, e espaçamento largo em caixa alta separa tanto as letras que o olho
para de ler a palavra e passa a soletrar. Agora é 12px com 0,16em, e num lugar
só — estava escrito à mão em dezenove pontos do código.

O cinza de apoio subiu junto, e os corpos de texto foram de 14 para 16/18px.

## Menos coisa na tela, sem perder nada

Você pediu mais minimalismo. Nada foi removido; mudou a ordem em que se
oferece:

| | Antes | Agora |
|---|---|---|
| Projetos | 4 × três parágrafos + duas fotos, tudo aberto | nome, resultado e link; o resto atrás de um clique |
| Habilidades | 28 caixinhas numa parede só | 9 por padrão, com filtro e contador `9 / 28` |
| Como eu trabalho | 6 evidências longas em sequência | a afirmação à vista, a prova em um clique |
| Currículo | inteiro no meio da página | fechado, com Baixar / Imprimir / Abrir |

## O fundo virou teia de verdade

Era a malha de pontos ligados que todo portfólio tem. Agora tem teia com raios
e anéis que cedem entre um raio e outro, ancorada nos cantos, com parallax e
gotas vermelhas. Clicar em qualquer lugar dá um puxão que se propaga.

As teias são desenhadas **uma vez** num canvas fora da tela e depois só
copiadas — redesenhar três teias de 13 raios a 60fps queimaria o celular para
produzir exatamente a mesma imagem.

## Duas aranhas andando na home

Andam em trechos: escolhem um ponto, caminham, param alguns segundos, escolhem
outro. A pausa é o que faz parecer bicho em vez de ícone deslizando.

Elas vivem **dentro da caixa da home**, não coladas na tela. A primeira versão
era `fixed` e acompanhava a rolagem como um cursor, precisando sumir no ar ao
sair do topo. Agora saem de cena pelo topo junto com a seção, como qualquer
outro elemento.

## A cor

Vermelho aranha — que já existia escondido no brilho da máscara do retrato.
Três variantes, e a de texto tem contraste de 6,4:1: o vermelho cheio dava
4,8:1, que passa no mínimo da WCAG raspando, e raspar não é margem em tela de
celular no sol.

Ele marca o que é interativo e o que orienta a leitura. De propósito **não**
significa "nível alto" em habilidades — cor com dois sentidos obriga quem lê a
adivinhar qual deles vale em cada lugar.

---

## Os bugs do dia

### O site não abria em desenvolvimento

Sobrou do projeto Next.js uma linha em `profile.ts`:

```ts
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "...";
```

`process` não existe no navegador. No build de produção passava despercebido
porque a constante não era usada e o Vite a removia; em dev não há essa
remoção, a linha é avaliada, e o React não monta. **Tela preta sem erro
visível.** Foi o que você viu quando o servidor local "não rodava".

### O menu não acompanhava a página

O cabeçalho era `sticky` e não funcionava. A causa estava longe dele: o
container raiz tinha `overflow-x-hidden`, e `overflow: hidden` transforma o
elemento em contêiner de rolagem — `sticky` gruda no contêiner de rolagem mais
próximo, não na janela. Com a página em 1200px, o topo do cabeçalho estava
medindo **-1200**. Trocado por `overflow-x-clip`, que corta igual sem criar
contêiner.

### O cabeçalho piscava perto do topo

Limiar único de 40px. Encolher o cabeçalho tira 44px de altura do topo da
página — **mais que a distância até a fronteira**. Encolhia, o conteúdo subia,
a posição voltava para baixo do limiar, crescia de novo, várias vezes por
segundo. O efeito desfazia a própria causa.

Agora encolhe acima de 96px e só volta a crescer abaixo de 24px. Os 72px de
folga são maiores que os 44px que a mudança desloca, então o laço não fecha.

### O currículo abria na impressão e não fechava mais

Para o PDF sair certo, o currículo se reabre sozinho no evento `beforeprint`.
Eu ligava e nunca desligava. Depois de imprimir, o botão "Fechar" funcionava
perfeitamente e a tela não mudava nada, porque a condição era
`aberto || imprimindo`. Faltava ouvir o `afterprint`.

### O caso "Este portfólio" descrevia o projeto errado

O card dizia `Next.js 16 · next-intl · Vitest · Playwright` e afirmava
"renderizado no servidor, legível sem JavaScript". Nada disso é verdade neste
site, que é Vite e não renderiza sem JS — era a descrição do projeto Next.js
apagado hoje de manhã.

Num portfólio que tem *"não publico dado que não posso confirmar"* escrito como
qualidade, isso era caro. Reescrito com o que dá para conferir no código.

---

## O PDF do currículo

Com o currículo nascendo fechado, ele sairia com dois botões e nada mais. O
gerador agora carrega a página com `?full=1` e tem uma trava que quebra o build
se o currículo não abrir — a falha mais cara aqui seria gerar em silêncio um
PDF bonito e vazio.

Saem **dois arquivos**, um por idioma. Um recrutador que lê a página em inglês
e baixa um currículo em português recebe um documento que não consegue avaliar.

De quebra: o resumo estava imprimindo antes do nome. O documento agora começa
por quem você é.

---

## No ar, e com endereço novo

Publicado, e o site ganhou um endereço mais apresentável:
**bryansoutodev.vercel.app**.

Não foi de primeira. O comando `vercel alias set` respondeu "Success!" e o
alias apareceu na listagem — mas o endereço dava 302 para a tela de login da
Vercel. Alias criado assim conta como URL de deploy, e a Proteção de Deploy
bloqueia essas. A correção foi registrar como *domínio do projeto*, aí ele
entra na categoria produção. O endereço antigo continua funcionando.

O `bryansouto.dev` está livre por US$ 9,99/ano. Não comprei — é o seu cartão.

## As notas do PageSpeed

| | Desempenho | Acessibilidade | Boas práticas | SEO |
|---|---|---|---|---|
| Portfólio (desktop) | 100 | 100 | 100 | 100 |
| Portfólio (celular) | 97 | 100 | 100 | 100 |
| Meteoros (desktop) | 100 | 100 | 100 | 100 |
| NYO (celular) | 87 | 100 | 100 | 100 |

Os 97 do celular vêm de uma métrica só, o Speed Index, que mede quando a tela
termina de se montar. É o preço da cortina de abertura e dos efeitos de entrada
— encenação, não lentidão. Não vale trocar a identidade do site por três
pontos.

O NYO em 87 é o único que pediria trabalho: LCP de 3,9s, que é o carrossel do
topo baixando imagem grande. Fica para outra sessão.

---

# A passada final: QA, otimização e segurança

## A suíte de testes que não existia

O Playwright estava instalado no projeto desde o começo e **nenhum teste tinha
sido escrito**. Agora são **60 testes** em cinco arquivos, rodando em desktop e
celular a cada `npm test`.

Eles não cobrem "tudo": cobrem, um por um, os defeitos que este site já teve.
Cada teste carrega no comentário a data e o motivo — o cabeçalho que não
grudava, o currículo que não fechava depois de imprimir, a cortina que ficava
de pé, o marcador `__PENDENTE__` que não pode chegar à tela.

Um detalhe que vale saber: **a suíte roda contra o build de produção**, não
contra o servidor de desenvolvimento. É o artefato que vai para a Vercel. E há
um arquivo só (`dev.spec.ts`) que faz o contrário e sobe o servidor de
desenvolvimento — por causa do `process.env` desta manhã, que quebrava em dev e
passava batido no build.

## O defeito que os testes acharam

**A cortina de abertura ficava de pé para quem rolasse a página enquanto ela
carregava.**

O mecanismo: o "avise quando terminar" chegava ao Loader como uma função criada
a cada renderização da página. A página se re-renderiza a cada rolagem, porque
guarda qual seção está ativa no menu e o tamanho do cabeçalho. Como o efeito do
Loader dependia daquela função, cada rolagem o desmontava — limpando o
cronômetro **e o teto de segurança** — e recomeçava do zero.

Rolar segurava a cortina. Rolar sem parar segurava para sempre. E a cortina é
uma camada que cobre a tela inteira: enquanto está lá, ela come todo clique da
página sem nenhum sinal visual de que é ela que está no caminho.

Medido antes e depois, com a página sendo rolada o tempo todo:

| | Cortina saiu em |
|---|---|
| Antes | **3708 ms**, e seguia enquanto houvesse rolagem |
| Depois | **~1500 ms**, independente da rolagem |

Ninguém percebe isso programando, porque programando você recarrega e espera.
Quem rola nos primeiros segundos é o visitante impaciente — que é a descrição
exata de um recrutador com trinta abas abertas.

Honestidade sobre o processo: o primeiro teste que escrevi *passava* com o
defeito presente, porque ele rolava e só então conferia — e assim que a rolagem
parava, o cronômetro corria inteiro. Reescrevi para rolar enquanto observa,
confirmei que reprova no código antigo e passa no novo, e só então dei por
resolvido.

## Segurança

Você pediu proteção contra SQL injection. Vou ser direto: **não se aplica
aqui**, e a razão importa.

Não é que esteja bem defendido — é que não existe banco de dados, não existe
servidor, não existe consulta. O site é um punhado de arquivos estáticos numa
CDN. Não há nada atrás para injetar. O mesmo vale para CSRF, autenticação
quebrada, upload malicioso e vazamento de dados: nada disso tem onde acontecer,
porque não há formulário, login nem servidor. Fingir o contrário daria uma
sensação de proteção contra um ataque inexistente.

O que **sobra** e é real: XSS, clickjacking e cadeia de suprimentos. Foi nisso
que trabalhei.

**A política de conteúdo (CSP)** agora diz `script-src 'self'` — sem
`unsafe-inline`, sem `unsafe-eval`. Só executa JavaScript vindo de arquivo
deste site. Script injetado por qualquer caminho simplesmente não roda.

Isso exigiu uma mudança: a página tinha um script inline (a linha que marcava
`js-reveal` antes da primeira pintura). Ele foi para o `main.tsx`, onde roda
igualmente cedo. Com zero scripts inline, a política pôde ser fechada em vez de
abrir exceção.

Também entraram `frame-ancestors 'none'` e `X-Frame-Options: DENY` contra
clickjacking, `nosniff`, `Referrer-Policy`, e um `Permissions-Policy` que
desliga câmera, microfone, localização e mais doze recursos que um portfólio
não usa.

O `npm audit` acusa **0 vulnerabilidades**, e o site tem só duas dependências
de produção — React e ReactDOM. Nenhum script de terceiro, nenhuma fonte
externa, nenhum analytics. Conferido no bundle final.

Tudo isso está explicado em `docs/seguranca.md`, com o porquê de cada
cabeçalho — para quando alguém for removê-los no futuro para "resolver" algum
problema.

**Um cabeçalho continua aberto de propósito:** `style-src` aceita estilo
inline. O React escreve atributos `style` em elementos vivos — a posição das
aranhas, a inclinação do retrato, a barra de progresso. Fechar quebraria o
site, e o risco é muito menor: CSS injetado não executa código, no pior caso
desconfigura o visual.

## Otimização

O cache dos arquivos com hash no nome virou eterno (`immutable`). Como o nome
do arquivo já contém o hash do conteúdo, o navegador de quem volta ao site não
precisa nem perguntar se mudou.

## Como conferir tudo

```bash
npm test               # 60 testes, desktop e celular
npm run check:headers  # confere os cabeçalhos do site NO AR
npm run build          # tipos + build
```

O `check:headers` é um script separado de propósito: os cabeçalhos vêm da borda
da Vercel, e o servidor local não os aplica. Um teste local passaria feliz com
a produção desprotegida.

---

## Onde está

| | |
|---|---|
| **Pronto** | Bilíngue, legível, minimalista, teia no fundo, aranhas, cor de destaque, PDF nos dois idiomas, 60 testes, cabeçalhos de segurança, 100/100/100/100 no desktop |
| **Falta** | Só o domínio próprio |

## Para quando você quiser

- Comprar o `bryansouto.dev` (US$ 9,99/ano) e apontar — é o mesmo passo do
  endereço de hoje, mais o DNS
- Revisar os níveis em `src/content/skills.ts` — é você que responde por eles
  na entrevista técnica
- O carrossel do NYO, para tirar aqueles 87 da faixa amarela
